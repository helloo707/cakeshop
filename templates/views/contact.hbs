<!DOCTYPE html>
<html lang="en">

<head>
    {{>headers}}
    <style>
        .contact-box {
            width: 50vw;
            height: 60vh;
            position: relative;
            margin: 15vh auto;
            background-color: aliceblue;
            padding: 5px 5px;
            border-radius: 5px;
            overflow: hidden;
            box-shadow: 0px 0px 30px 9px rgb(202 118 145 / 78%);
        }

        .hero {
            height: 100%;
            width: 100%;
            background-image: linear-gradient(rgba(255, 119, 119, 0.244), rgba(232, 230, 230, 0.353)), url(/img/pxfuel.jpg);
            background-position: center;
            background-size: cover;
            position: absolute;
        }

        .input-grp {
            top: 10vh;
            position: absolute;
            width: 40vw;
            transition: 0.5s;
            margin: 10px 50px;
        }

        .input-field {
            width: 100%;
            padding: 10px 0;
            border-top: 0;
            border-right: 0;
            border-left: 0;
            border-bottom: 1px solid rgb(202 118 145 / 78%);
            outline: none;
            background: transparent;
            bottom: 10px;
        }

        .submit-btn {
            width: 40%;
            padding: 10px 30px;
            cursor: pointer;
            display: block;
            margin: 30px auto;
            background: linear-gradient(to right, #ff4c15, #ff9a81);
            border: 0;
            outline: none;
            border-radius: 20px;
        }

        #message {
            height: 10vh;
            width: 40vw;
            position: relative;
            margin-top: 20px;
        }

        footer {
            margin-top: 100vh !important;
        }
    </style>
</head>

<body>
    {{>navbar}}
    <div class="hero">
        <div class="contact-box">
            <form action="https://formspree.io/f/mnqkqnwd"  method="POST" class="input-grp">
                <input type="email" id="email" class="input-field" placeholder="Email Id" required>
                <input type="message" id="number" class="input-field" placeholder="Contact Number" required>
                <input type="text" id="Name" class="input-field" placeholder="Name" required>
                <textarea name="message" id="message" cols="30" rows="10" placeholder="How can we help you?"></textarea>
                <button type="submit" class="submit-btn">Submit Now!!!</button>
            </form>
        </div>
    </div>
    <footer>
        {{>footer}}
    </footer>
</body>

</html>